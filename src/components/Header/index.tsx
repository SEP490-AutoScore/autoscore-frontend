import React from 'react';
import "./style.css";

const Header: React.FC = () => {
  return (
    <header className="header">
      <h1>Application Header</h1>
      {/* Add logo or other header items here */}
    </header>
  );
};

export default Header;
